
  # SAPOM Form Input Pesanan

  This is a code bundle for SAPOM Form Input Pesanan. The original project is available at https://www.figma.com/design/lqr4lJeAWAUX62129MIePY/SAPOM-Form-Input-Pesanan.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  